# 00 — API -> Postgres ingestion (draft)

## Goals
- понять ingestion-паттерны: pagination, retries, idempotency
- сложить raw в Postgres (JSONB) и сделать простую нормализацию

## Stack
- `make up STACK=postgres`
